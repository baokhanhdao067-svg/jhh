-- Khanh Dz Ultra Hub v3.0 (Silent Aim + Auto Snap Head 502)
local P, L, U, R = game:GetService("Players"), game:GetService("Lighting"), game:GetService("UserInputService"), game:GetService("RunService")
local LP, Cam = P.LocalPlayer, workspace.CurrentCamera

local Config = {
    Aim = false, SnapHead = true, AimSmooth = 1.0, FOV = 300, FOVShow = true,
    HitboxScale = 1.0, HitboxTrans = 0.4,
    ESP_Box = false, ESP_Tracers = false, ESP_Info = false,
    AFK_FPS = false, AntiAFK = true, NoShadows = false, NoParticles = false, NoTextures = false
}

local lastIn, isAFK = os.time(), false

-- Safe FPS Setter
local function safeSetFPS(cap)
    pcall(function()
        if setfpscap then setfpscap(cap)
        elseif setbackbufferfps then setbackbufferfps(cap) end
    end)
end

-- Anti AFK
if Config.AntiAFK then
    local VirtualUser = game:GetService("VirtualUser")
    LP.Idled:Connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end)
end

-- ScreenGui Container
local Gui = Instance.new("ScreenGui")
Gui.Name, Gui.ResetOnSpawn = "KhanhDz_UltraHub_v30", false

pcall(function()
    if gethui then Gui.Parent = gethui()
    elseif syn and syn.protect_gui then syn.protect_gui(Gui); Gui.Parent = game:GetService("CoreGui")
    else Gui.Parent = game:GetService("CoreGui") end
end)
if not Gui.Parent then Gui.Parent = LP:WaitForChild("PlayerGui") end

-- Icon Toggle
local TBtn = Instance.new("ImageButton", Gui)
TBtn.Size, TBtn.Position, TBtn.BackgroundColor3 = UDim2.new(0,50,0,50), UDim2.new(0,15,0,150), Color3.fromRGB(15,15,20)
TBtn.Image, TBtn.Active, TBtn.Draggable = "rbxassetid://6031068426", true, true
Instance.new("UICorner", TBtn).CornerRadius = UDim.new(1,0)
local TStroke = Instance.new("UIStroke", TBtn)
TStroke.Color, TStroke.Thickness = Color3.fromRGB(255,50,50), 2

-- Main Frame
local Main = Instance.new("Frame", Gui)
Main.Size, Main.Position, Main.BackgroundColor3 = UDim2.new(0,270,0,500), UDim2.new(0,75,0,150), Color3.fromRGB(18,18,24)
Main.Active, Main.Draggable, Main.Visible = true, true, false
Instance.new("UICorner", Main).CornerRadius = UDim.new(0,10)
local MStroke = Instance.new("UIStroke", Main)
MStroke.Color, MStroke.Thickness = Color3.fromRGB(255,50,50), 1.5

-- Title
local Title = Instance.new("TextLabel", Main)
Title.Size, Title.Position, Title.Text = UDim2.new(1,-40,0,40), UDim2.new(0,15,0,0), "KHANH DZ HUB <font color=\"#FF3232\">v3.0 Silent</font>"
Title.RichText, Title.TextColor3, Title.Font, Title.TextSize, Title.TextXAlignment = true, Color3.fromRGB(255,255,255), Enum.Font.GothamBold, 15, Enum.TextXAlignment.Left
Title.BackgroundTransparency = 1

TBtn.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

-- Search Box
local SearchBox = Instance.new("TextBox", Main)
SearchBox.Size, SearchBox.Position = UDim2.new(1,-20,0,30), UDim2.new(0,10,0,40)
SearchBox.PlaceholderText, SearchBox.Text = "🔍 Tìm tính năng...", ""
SearchBox.BackgroundColor3, SearchBox.TextColor3, SearchBox.PlaceholderColor3 = Color3.fromRGB(28,28,36), Color3.fromRGB(255,255,255), Color3.fromRGB(120,120,140)
SearchBox.Font, SearchBox.TextSize = Enum.Font.Gotham, 12
Instance.new("UICorner", SearchBox).CornerRadius = UDim.new(0,6)

-- Scrolling Frame
local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size, Scroll.Position, Scroll.CanvasSize = UDim2.new(1,-10,1,-80), UDim2.new(0,5,0,75), UDim2.new(0,0,0,0)
Scroll.BackgroundTransparency, Scroll.ScrollBarThickness = 1, 3
local Layout = Instance.new("UIListLayout", Scroll)
Layout.HorizontalAlignment, Layout.Padding = Enum.HorizontalAlignment.Center, UDim.new(0,6)
Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Scroll.CanvasSize = UDim2.new(0,0,0, Layout.AbsoluteContentSize.Y + 15)
end)

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    local filter = SearchBox.Text:lower()
    for _, item in pairs(Scroll:GetChildren()) do
        if item:IsA("Frame") or item:IsA("TextButton") then
            item.Visible = (filter == "" or item.Name:lower():find(filter))
        end
    end
end)

-- UI Creators
local function AddToggle(name, default, callback)
    local B = Instance.new("TextButton", Scroll)
    B.Name = name
    B.Size, B.BackgroundColor3 = UDim2.new(0,240,0,35), default and Color3.fromRGB(200,40,40) or Color3.fromRGB(32,32,42)
    B.Text, B.TextColor3, B.Font, B.TextSize = "  "..name, Color3.fromRGB(220,220,220), Enum.Font.GothamSemibold, 12
    B.TextXAlignment = Enum.TextXAlignment.Left
    Instance.new("UICorner", B).CornerRadius = UDim.new(0,6)
    
    local Indicator = Instance.new("Frame", B)
    Indicator.Size, Indicator.Position = UDim2.new(0,14,0,14), UDim2.new(1,-24,0.5,-7)
    Indicator.BackgroundColor3 = default and Color3.fromRGB(255,50,50) or Color3.fromRGB(80,80,95)
    Instance.new("UICorner", Indicator).CornerRadius = UDim.new(1,0)

    local st = default
    B.MouseButton1Click:Connect(function()
        st = not st
        B.BackgroundColor3 = st and Color3.fromRGB(200,40,40) or Color3.fromRGB(32,32,42)
        Indicator.BackgroundColor3 = st and Color3.fromRGB(255,50,50) or Color3.fromRGB(80,80,95)
        callback(st)
    end)
end

local function AddSlider(name, min, max, default, unit, callback)
    local Cont = Instance.new("Frame", Scroll)
    Cont.Name = name
    Cont.Size, Cont.BackgroundTransparency = UDim2.new(0,240,0,45), 1

    local Lbl = Instance.new("TextLabel", Cont)
    Lbl.Size, Lbl.Text = UDim2.new(1,0,0,18), string.format("%s: %d%s", name, default, unit)
    Lbl.BackgroundTransparency, Lbl.TextColor3, Lbl.Font, Lbl.TextSize = 1, Color3.fromRGB(180,180,200), Enum.Font.Gotham, 11
    Lbl.TextXAlignment = Enum.TextXAlignment.Left

    local SFrame = Instance.new("Frame", Cont)
    SFrame.Size, SFrame.Position, SFrame.BackgroundColor3 = UDim2.new(1,0,0,8), UDim2.new(0,0,0,22), Color3.fromRGB(35,35,45)
    Instance.new("UICorner", SFrame).CornerRadius = UDim.new(1,0)

    local valPct = (default - min) / (max - min)
    local SFill = Instance.new("Frame", SFrame)
    SFill.Size, SFill.BackgroundColor3 = UDim2.new(valPct, 0, 1, 0), Color3.fromRGB(255,50,50)
    Instance.new("UICorner", SFill).CornerRadius = UDim.new(1,0)

    local dragging = false
    local function update(input)
        local pct = math.clamp((input.Position.X - SFrame.AbsolutePosition.X) / SFrame.AbsoluteSize.X, 0, 1)
        SFill.Size = UDim2.new(pct, 0, 1, 0)
        local val = math.floor(min + (max - min) * pct)
        Lbl.Text = string.format("%s: %d%s", name, val, unit)
        callback(val)
    end

    SFrame.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dragging = true; update(i)
        end
    end)
    U.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dragging = false end
    end)
    U.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then update(i) end
    end)
end

-- FOV Circle Visual
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(255, 50, 50)
FOVCircle.Transparency = 0.7
FOVCircle.Filled = false
FOVCircle.Visible = false

-- Controls Setup
AddToggle("Đạn Dính Đầu (Keo 502)", false, function(v) Config.Aim = v end)
AddToggle("Quay Hướng Khác Vẫn Ghim", true, function(v) Config.SnapHead = v end)
AddToggle("Hiển Thị Vòng Hút Đạn", true, function(v) Config.FOVShow = v end)
AddSlider("Bán Kính Hút Đạn", 50, 600, Config.FOV, "px", function(v) Config.FOV = v end)
AddSlider("Tốc Độ Khóa (Instant)", 1, 10, 10, "", function(v) Config.AimSmooth = v / 10 end)

AddSlider("Tỷ Lệ Phóng To Đầu", 100, 500, 100, "%", function(v) Config.HitboxScale = v / 100 end)
AddSlider("Độ Trong Suốt Đầu", 0, 100, 40, "%", function(v) Config.HitboxTrans = v / 100 end)

AddToggle("Định Vị Khung (Box ESP)", false, function(v) Config.ESP_Box = v end)
AddToggle("Định Vị Đường Kẻ (Tracers)", false, function(v) Config.ESP_Tracers = v end)
AddToggle("Định Vị Máu & Tên (Info)", false, function(v) Config.ESP_Info = v end)

AddToggle("AFK Hạ 15 FPS Auto", false, function(v) Config.AFK_FPS = v; if not v then safeSetFPS(60) end end)
AddToggle("Tắt Đổ Bóng & Ánh Sáng", false, function(v)
    Config.NoShadows = v; L.GlobalShadows = not v
    if v then pcall(function() L.Technology = Enum.Technology.Compatibility end) end
end)
AddToggle("Xóa Hiệu Ứng (Particles)", false, function(v)
    Config.NoParticles = v
    if v then
        for _,e in pairs(workspace:GetDescendants()) do
            if e:IsA("ParticleEmitter") or e:IsA("Smoke") or e:IsA("Fire") or e:IsA("Sparkles") then e.Enabled = false end
        end
    end
end)
AddToggle("Siêu Fix Lag (Smooth Textures)", false, function(v)
    Config.NoTextures = v
    if v then
        pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
        for _,e in pairs(workspace:GetDescendants()) do
            if e:IsA("BasePart") then e.Material = Enum.Material.SmoothPlastic
            elseif e:IsA("Decal") or e:IsA("Texture") then e:Destroy() end
        end
    end
end)

------------------- CORE KEO 502 & SNAP LOGIC -------------------

-- Thuật toán tìm Đầu gần nhất (Bất chấp hướng quay)
local function getAbsoluteClosestHead()
    local closestHead = nil
    local shortestDist = math.huge
    local centerScreen = Vector2.new(Cam.ViewportSize.X / 2, Cam.ViewportSize.Y / 2)

    for _, p in pairs(P:GetPlayers()) do
        if p ~= LP and p.Character then
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            local head = p.Character:FindFirstChild("Head")
            
            if hum and hum.Health > 0 and head then
                if Config.SnapHead then
                    -- Quét không gian 3D xung quanh (Quay hướng nào cũng bắt được)
                    local dist3D = (head.Position - Cam.CFrame.Position).Magnitude
                    if dist3D < shortestDist then
                        shortestDist = dist3D
                        closestHead = head
                    end
                else
                    -- Quét trong phạm vi FOV màn hình
                    local screenPos, onScreen = Cam:WorldToViewportPoint(head.Position)
                    if onScreen then
                        local dist2D = (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude
                        if dist2D < Config.FOV and dist2D < shortestDist then
                            shortestDist = dist2D
                            closestHead = head
                        end
                    end
                end
            end
        end
    end
    return closestHead
end

-- ESP System
local espCache = {}
local function createESP(plr)
    if plr == LP then return end
    local drawings = {
        Box = Drawing.new("Square"),
        Tracer = Drawing.new("Line"),
        Text = Drawing.new("Text")
    }
    drawings.Box.Thickness, drawings.Box.Color, drawings.Box.Filled = 1.5, Color3.fromRGB(255, 50, 50), false
    drawings.Tracer.Thickness, drawings.Tracer.Color = 1, Color3.fromRGB(255, 50, 50)
    drawings.Text.Size, drawings.Text.Center, drawings.Text.Outline, drawings.Text.Color = 13, true, true, Color3.fromRGB(255, 255, 255)
    espCache[plr] = drawings
end

for _, p in pairs(P:GetPlayers()) do createESP(p) end
P.PlayerAdded:Connect(createESP)
P.PlayerRemoving:Connect(function(p)
    if espCache[p] then
        for _, d in pairs(espCache[p]) do d:Remove() end
        espCache[p] = nil
    end
end)

-- Main Loop
R.RenderStepped:Connect(function()
    -- Cập nhật FOV Ring
    FOVCircle.Position = Vector2.new(Cam.ViewportSize.X / 2, Cam.ViewportSize.Y / 2)
    FOVCircle.Radius = Config.FOV
    FOVCircle.Visible = Config.Aim and Config.FOVShow

    -- Xử lý Aimbot Snap Head (Keo 502)
    if Config.Aim then
        local targetHead = getAbsoluteClosestHead()
        if targetHead then
            local lockCFrame = CFrame.lookAt(Cam.CFrame.Position, targetHead.Position)
            if Config.AimSmooth >= 1 then
                Cam.CFrame = lockCFrame -- Ghim tức thì 100%
            else
                Cam.CFrame = Cam.CFrame:Lerp(lockCFrame, Config.AimSmooth)
            end
        end
    end

    -- Xử lý Phóng to Đầu người (Hitbox Expander)
    for _, p in pairs(P:GetPlayers()) do
        if p ~= LP and p.Character then
            local head = p.Character:FindFirstChild("Head")
            if head then
                if not head:FindFirstChild("OrigSize") then
                    local orig = Instance.new("Vector3Value", head)
                    orig.Name = "OrigSize"
                    orig.Value = head.Size
                end
                head.Size = head.OrigSize.Value * Config.HitboxScale
                head.CanCollide = false
                head.Transparency = Config.HitboxScale > 1 and Config.HitboxTrans or 0
            end
        end
    end

    -- Render ESP
    for plr, d in pairs(espCache) do
        if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and plr.Character:FindFirstChildOfClass("Humanoid") then
            local hrp = plr.Character.HumanoidRootPart
            local hum = plr.Character:FindFirstChildOfClass("Humanoid")
            local pos, onScreen = Cam:WorldToViewportPoint(hrp.Position)

            if onScreen and hum.Health > 0 then
                local head = plr.Character:FindFirstChild("Head")
                local headPos = head and Cam:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0)) or pos
                local height = math.abs(headPos.Y - pos.Y) * 2
                local width = height / 1.5

                if Config.ESP_Box then
                    d.Box.Size = Vector2.new(width, height)
                    d.Box.Position = Vector2.new(pos.X - width / 2, pos.Y - height / 2)
                    d.Box.Visible = true
                else d.Box.Visible = false end

                if Config.ESP_Tracers then
                    d.Tracer.From = Vector2.new(Cam.ViewportSize.X / 2, Cam.ViewportSize.Y)
                    d.Tracer.To = Vector2.new(pos.X, pos.Y)
                    d.Tracer.Visible = true
                else d.Tracer.Visible = false end

                if Config.ESP_Info then
                    local dist = math.floor((hrp.Position - Cam.CFrame.Position).Magnitude)
                    d.Text.Text = string.format("%s [%d m]\nHP: %d/%d", plr.Name, dist, math.floor(hum.Health), hum.MaxHealth)
                    d.Text.Position = Vector2.new(pos.X, pos.Y - height / 2 - 25)
                    d.Text.Visible = true
                else d.Text.Visible = false end
            else
                d.Box.Visible, d.Tracer.Visible, d.Text.Visible = false, false, false
            end
        else
            d.Box.Visible, d.Tracer.Visible, d.Text.Visible = false, false, false
        end
    end
end)

-- Auto AFK Manager
local function resetAFKTimer()
    lastIn = os.time()
    if isAFK then isAFK = false; safeSetFPS(60) end
end
U.InputBegan:Connect(resetAFKTimer)
U.InputChanged:Connect(resetAFKTimer)

task.spawn(function()
    while task.wait(1) do
        if Config.AFK_FPS and (os.time() - lastIn >= 15) and not isAFK then
            isAFK = true
            safeSetFPS(15)
        end
    end
end)
